<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> {{ pageTitle }} </title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #f8f9fa;
        }
        .container {
            width: 500px;
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 20px;
            background-color: #fff;
            text-align: center;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .tabs {
            display: flex;
            justify-content: center;
            margin-bottom: 20px;
        }
        .tab {
            padding: 10px 20px;
            cursor: pointer;
            font-weight: bold;
            color: #333;
            text-decoration: none;
        }
        .tab.active, .tab:hover {
            border-bottom: 2px solid #4e73df;
            color: #4e73df;
        }
        h1 {
            font-size: 24px;
            margin-bottom: 20px;
        }
        p {
            font-size: 14px;
            color: #666;
            text-align: left;
        }
        .button {
            display: inline-block;
            padding: 10px 20px;
            margin: 10px 5px;
            background-color: #4e73df;
            color: white;
            border: none;
            border-radius: 5px;
            text-decoration: none;
            font-size: 16px;
            cursor: pointer;
        }
        .button:hover {
            background-color: #3b5998;
        }


    </style>
</head>
<body>
    <div class="container">
        <!-- Tabs Section -->
        <div class="tabs">
            <div class="tab active"><a href="index.php" class="tab">INSTRUCTIONS</a></div>
            <div class="tab"><a href="upload.php" class="tab">UPLOAD</a></div>
            <div class="tab"><a href="history.php" class="tab">HISTORY</a></div>
        </div>

        <!-- Main Content -->
        <h1>{{ pageTitle }}</h1>
        <p>Convert Colour Images to Black and White. Get your converted images sent directly to your email!</p>
        <p><strong>Instructions:</strong><br>
            Click <strong>UPLOAD</strong> to upload and convert a colour image to black and white<br>
            Click <strong>HISTORY</strong> to view the list of images uploaded to this service and their status. You can access the originals in this page
        </p>

        <!-- Buttons Section -->
        <a href="upload.php" class="button">Upload</a>
        <a href="history.php" class="button">History</a>
    </div>
</body>
</html>
